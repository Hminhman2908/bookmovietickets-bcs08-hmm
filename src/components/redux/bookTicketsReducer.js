import { chairArr } from "../data";

const initialState = {
  chairArr: chairArr,
};

export let bookTicketsReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
